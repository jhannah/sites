<section class="bg-light-paper py-12 px-4">
  <div class="max-w-[700px] m-auto">
    <h2 class="text-[clamp(1rem,_10vw,_5rem)]">{{ en.event.intro.title }}</h2>
    {% for paragraph in en.event.intro.content %}
      <p class="text-2xl mt-4">{{ paragraph }}</p>
    {% endfor %}
  </div>
</section>
